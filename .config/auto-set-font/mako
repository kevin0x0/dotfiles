
set_font() {
  config_dir="${XDG_CONFIG_HOME:-$HOME/.config}/mako"
  for f in "$config_dir/config-light" "$config_dir/config-dark"; do
    sed -- 's/^\(font=\)[^,]*,/\1'"$monofont"',/' "$f" >"$tmpfile" \
      && cp -- "$tmpfile" "$f"
  done
}

reload() {
  makoctl reload
}

# vim: ft=sh
