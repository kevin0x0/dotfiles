
set_font() {
  local font=$1 monofont=$2
  local f="$XDG_CONFIG_HOME/swayimg/config"
  sed -i '/^\[font\]$/!b; :a; n; /^name/!ba; c\
name = '"$monofont" "$f" 
}

reload() {
  return
}

# vim: ft=bash
