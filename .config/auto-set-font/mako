
set_font() {
  f="${XDG_CONFIG_HOME:-$HOME/.config}/mako/config"
  sed -- 's/^\(font=\)[^,]*,/\1'"$monofont"',/' "$f" >"$tmpfile" \
    && cp -- "$tmpfile" "$f"
}

reload() {
  makoctl reload
}

# vim: ft=bash
