COLORSCHEME_DIR=$(realpath -- "${XDG_STATE_HOME:-~/.local/state}/colorscheme/mako/colorscheme")
mkdir -p "$(dirname -- "$COLORSCHEME_DIR")"
ln -sf "$COLORSCHEME_DIR/catppuccin-mocha" "$COLORSCHEME_DIR"

# vim: ft=sh
