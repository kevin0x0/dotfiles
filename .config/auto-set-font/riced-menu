
set_font() {
  f="${XDG_CONFIG_HOME:-$HOME/.config}/riced-menu"
  sed -- 's/^\(FONTFAMILY=\).*$/\1'"'$monofont'"'/' "$f" >"$tmpfile" \
    && cp -- "$tmpfile" "$f"
}

reload() {
  return
}

# vim: ft=sh
