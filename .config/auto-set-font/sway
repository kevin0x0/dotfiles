
set_font() {
  f="${XDG_CONFIG_HOME:-$HOME/.config}/sway/config.d/visual"
  sed -e 's/^\(set \$font\) .*$/\1 '"$font"'/' \
    -e 's/^\(set \$monofont\) .*$/\1 '"$monofont"'/' \
    -- "$f" >"$tmpfile" \
    && cp -- "$tmpfile" "$f"
}

reload() {
  swaymsg reload
}

# vim: ft=bash
