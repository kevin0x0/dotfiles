set -e

COLORSCHEME_DIR=${XDG_CONFIG_HOME:-$HOME/.config}/mako/colorscheme
COLOR_THEME_FILE=$COLORSCHEME_STATE_HOME/mako/colorscheme

theme=$1
ln -sf -- "$COLORSCHEME_DIR/$theme" "$COLOR_THEME_FILE"

makoctl reload || true
# vim: ft=sh
