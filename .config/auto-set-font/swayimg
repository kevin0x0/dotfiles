
set_font() {
  local font="$1" monofont="$2"
  local f="${XDG_CONFIG_HOME:-$HOME/.config}/swayimg/config"
  local tmpfile="$(mktemp)"
  sed '/^\[font\]$/!b; :a; n; /^name/!ba; c\
name = '"$monofont" -- "$f" >"$tmpfile" && mv -- "$tmpfile" "$f"
}

reload() {
  return
}

# vim: ft=bash
