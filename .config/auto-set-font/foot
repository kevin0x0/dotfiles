
set_font() {
  local font="$1" monofont="$2"
  local f="${XDG_CONFIG_HOME:-$HOME/.config}/foot/foot.ini"
  local tmpfile="$(mktemp)"
  sed 's/^\(font=\)[^-:]*\([-:]\)/\1'"$monofont"'\2/' -- "$f" \
    >"$tmpfile" && mv -- "$tmpfile" "$f"
}

reload() {
  return
}

# vim: ft=bash
