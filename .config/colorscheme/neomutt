set -e

COLOR_THEME_FILE=$COLORSCHEME_STATE_HOME/neomutt/colorscheme
if [ ! -e  "$(dirname -- $COLOR_THEME_FILE)" ]; then
  if ! mkdir -p "$(dirname -- $COLOR_THEME_FILE)"; then
    echo "can not create state file for neumutt" >&2
    exit 1
  fi
fi

theme=$1
printf "%s\n" "$theme" > "$COLOR_THEME_FILE"

# Unfortunately, it seems there is no simple solution to notify running neomutt
# process to reload color config.
# vim: ft=sh
