
FONTFAMILY='monospace'
# Font by fontconfig format
FONT="$FONTFAMILY, Bold 14"

colorfile="${XDG_STATE_HOME:-$HOME/.local/state}/colorscheme/riced-menu/colorscheme"

COLORS=
if [ -r "$colorfile" ]; then
  . "$colorfile"
fi

# vim: ft=bash
