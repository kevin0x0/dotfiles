#!/usr/bin/bash

shopt -s extglob

file=$(realpath -e -- "$1") || exit 1
file=${file#x}
mime_type=$(file --mime-type --brief -- "$f" </dev/null)
case "$mime_type" in
  text/plain)
    cat -- "$file"
    ;;
  @(application/@(yaml|toml|json)|text/*))
    highlight -O ansi -- "$f"
    ;;
  *)
    content=$(file --brief -- "$f")
    length=${#content}
    width=$(($2 - 3))
    if [ "$width" -le 0 ]; then
      exit
    fi
    for ((i = 0; i < length; i += this_width)); do
      this_width=$((i + width < length ? width : length - i))
      printf "%s\n" "${content:$i:$this_width}"
    done
    ;;
esac
