
set_font() {
  local font="$1" monofont="$2"
  local f="${XDG_CONFIG_HOME:-$HOME/.config}/riced-menu"
  local tmpfile="$(mktemp)"
  sed 's/^\(FONTFAMILY=\).*$/\1'"'$monofont'"'/' -- "$f" \
    >"$tmpfile" && mv -- "$tmpfile" "$f"
}

reload() {
  return
}

# vim: ft=bash
