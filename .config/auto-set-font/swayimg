
set_font() {
  f="${XDG_CONFIG_HOME:-$HOME/.config}/swayimg/config"
  sed '/^\[font\]$/!b; :a; n; /^name/!ba; c\
name = '"$monofont" -- "$f" >"$tmpfile" && cp -- "$tmpfile" "$f"
}

reload() {
  return
}

# vim: ft=bash
