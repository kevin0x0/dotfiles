#!/bin/sh

if ! clash-api >/dev/null 2>&1; then
  echo "Clash is not started yet!"
  exit 1
fi

node="🔰 选择节点"

if ! current="$(clash-api "proxies/$node" | jq -r '.now')"; then
  exit 1
fi

printf "%s\n" "$current"
