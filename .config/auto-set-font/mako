
set_font() {
  local font="$1" monofont="$2"
  local f="${XDG_CONFIG_HOME:-$HOME/.config}/mako/config"
  local tmpfile="$(mktemp)"
  sed 's/^\(font=\)[^,]*,/\1'"$monofont"',/' -- "$f" \
    >"$tmpfile" && mv -- "$tmpfile" "$f"
}

reload() {
  makoctl reload
}

# vim: ft=bash
