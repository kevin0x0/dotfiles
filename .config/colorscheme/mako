set -e

CONFIG_FILE="${XDG_CONFIG_HOME:-$HOME/.config}/mako/config"

case $1 in
  dark)
    theme=config-dark
    ;;
  *)
    theme=config-light
    ;;
esac

ln -sf "$theme" "$CONFIG_FILE"

makoctl reload || true
# vim: ft=sh
