text/html; firefox-mailcap-wrapper %s; \
  test=command -v firefox >/dev/null && [ -n "$WAYLAND_DISPLAY$DISPLAY" ]

text/html; cat %s; copiousoutput; \
  test=! command -v firefox >/dev/null || [ -z "$WAYLAND_DISPLAY$DISPLAY" ]
