#!/usr/bin/dash

if ! clash-api >/dev/null 2>&1; then
  echo "Clash is not started yet!"
  exit 1
fi

node="🔰 选择节点"

if ! selected="$(clash-api "proxies/$node" | fj -r '.all[*]' | fzf)"; then
  exit 1
fi
printf "%s\n" "$selected"

clash-api -S "$selected" "proxies/$node"
